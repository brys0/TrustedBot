package de.pheromir.discordmusicbot.listener;

public class VoiceChannelListener extends ListenerAdapter {

	/*
	 * @Override public void onGuildVoiceJoin(GuildVoiceJoinEvent e) { if
	 * (!e.getChannelJoined().getGuild().getTextChannelsByName(Methods.
	 * transformVoiceChannelName(e.getChannelJoined().getName()),
	 * false).isEmpty()) { TextChannel stc =
	 * e.getChannelJoined().getGuild().getTextChannelsByName(Methods.
	 * transformVoiceChannelName(e.getChannelJoined().getName()), false).get(0);
	 * ArrayList<Permission> perms = new ArrayList<>();
	 * perms.add(Permission.MESSAGE_READ);
	 * perms.add(Permission.MESSAGE_HISTORY);
	 * perms.add(Permission.VOICE_CONNECT);
	 * stc.getManager().putPermissionOverride(e.getMember(), perms,
	 * null).complete(); } }
	 * 
	 * @Override public void onGuildVoiceLeave(GuildVoiceLeaveEvent e) { if
	 * (!e.getChannelLeft().getGuild().getTextChannelsByName(Methods.
	 * transformVoiceChannelName(e.getChannelLeft().getName()),
	 * false).isEmpty()) { TextChannel stc =
	 * e.getChannelLeft().getGuild().getTextChannelsByName(Methods.
	 * transformVoiceChannelName(e.getChannelLeft().getName()), false).get(0);
	 * stc.getManager().removePermissionOverride(e.getMember()).complete(); } }
	 */
	 
}